#ifndef INTERFACEDANCE_H
#define INTERFACEDANCE_H

typedef struct no Tno;
typedef struct lista TLista;
Tno* criarNo(int);
TLista* criarLista();
int inserir(TLista*, int);
int EDdanca(int, int);

#endif